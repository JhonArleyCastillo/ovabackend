# =============================================================================
# ARCHIVO DE EJEMPLO - VARIABLES DE ENTORNO BACKEND OVA
# =============================================================================
# Copia este archivo como .env y completa con los valores reales
# NUNCA subir el archivo .env real al repositorio

# =============================================================================
# ENTORNO DE EJECUCIÓN
# =============================================================================
ENVIRONMENT=development

# =============================================================================
# CONFIGURACIÓN DE BASE DE DATOS
# =============================================================================
# SQLite para desarrollo (más simple)
USE_SQLITE=true

# MySQL para producción (descomentar si usas MySQL)
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=tu_usuario_mysql
# DB_PASSWORD=tu_password_mysql
# DB_NAME=TuBD

# =============================================================================
# HUGGING FACE API CONFIGURATION
# =============================================================================
# Obtén tu API key de: https://huggingface.co/settings/tokens
HF_API_KEY=tu_huggingface_api_key_aqui

# Modelo de texto/chat
HF_MODEL=microsoft/DialoGPT-medium

# Modelo de lenguaje de señas

# =============================================================================
# CONFIGURACIÓN CORS
# =============================================================================
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://helpova.web.app
CORS_MAX_AGE=3600

# =============================================================================
# CONFIGURACIÓN JWT (CRÍTICA PARA SEGURIDAD)
# =============================================================================
# Clave para desarrollo (CAMBIAR EN PRODUCCIÓN)
# Generar una clave segura con: openssl rand -hex 32
JWT_SECRET_KEY=dev_secret_key_change_in_production_2025
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
DB_NAME=ovaweb_dev

# SQLite Option for Development (set to true to use SQLite instead of MySQL)
USE_SQLITE=false

# ===== HUGGING FACE API CONFIGURATION =====
# API Key para Hugging Face (requerido para funcionalidades de IA)
HF_API_KEY=your_huggingface_api_key_here

# Modelo de lenguaje de señas
HF_ASL_SPACE_URL=https://jhonarleycastillov-asl-image.hf.space
# Modelo general (opcional)
model=your_model_name_here

# ===== CORS CONFIGURATION =====
# Orígenes permitidos para CORS (separados por comas)
ALLOWED_ORIGINS=https://helpova.web.app,http://localhost:3000,https://3.15.5.52,https://3.15.5.52:8000,https://api.ovaonline.tech,http://api.ovaonline.tech

# Tiempo de vida máximo para preflight requests en segundos
CORS_MAX_AGE=3600

# ===== JWT CONFIGURATION =====
# Clave secreta para JWT (CAMBIAR EN PRODUCCIÓN)
JWT_SECRET_KEY=your_super_secret_jwt_key_here_change_in_production

# Algoritmo para JWT
JWT_ALGORITHM=HS256

# Tiempo de expiración del token en minutos
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ===== PRODUCTION ONLY VARIABLES =====
# Las siguientes variables son requeridas SOLO en producción:

# Configuración de base de datos en producción (reemplazar valores)
# DB_HOST=your_production_db_host
# DB_USER=your_production_db_user
# DB_PASSWORD=your_production_db_password
# DB_NAME=your_production_db_name

# API Keys de producción
# HF_API_KEY=your_production_hf_api_key

# JWT seguro para producción
# JWT_SECRET_KEY=your_production_super_secure_jwt_key

# CORS para producción (solo HTTPS)
# ALLOWED_ORIGINS=https://yourdomain.com,https://api.yourdomain.com

# ===== OPTIONAL CONFIGURATIONS =====
# Puerto del servidor (por defecto 8000)
# PORT=8000

# Nivel de logging: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO

# ===== INSTRUCTIONS =====
# 1. Copie este archivo como .env en la carpeta raíz del backend
# 2. Reemplace todos los valores "your_*_here" con sus valores reales
# 3. NUNCA cometa este archivo .env al repositorio por seguridad
# 4. En producción, use variables de entorno del servidor en lugar del archivo .env
