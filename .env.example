# =============================================================================
# ARCHIVO DE EJEMPLO - VARIABLES DE ENTORNO BACKEND OVA
# =============================================================================
# Copia este archivo como .env y completa con los valores reales
# NUNCA subir el archivo .env real al repositorio

# =============================================================================
# ENTORNO DE EJECUCIÓN
# =============================================================================
# development | production
ENVIRONMENT=development

# =============================================================================
# CONFIGURACIÓN DE BASE DE DATOS
# =============================================================================
# SQLite para desarrollo (más simple)
USE_SQLITE=true

# MySQL para producción (descomentar si usas MySQL)
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=mysql_user
# DB_PASSWORD=mysql_password
# DB_NAME=ovaweb

# =============================================================================
# HUGGING FACE API CONFIGURATION
# =============================================================================
# Obtén tu API key de: https://huggingface.co/settings/tokens
HF_API_KEY=your_huggingface_api_key_here

# Modelo de texto/chat
HF_MODEL=microsoft/DialoGPT-medium

HF_ASL_SPACE_URL=https://jhonarleycastillov-asl-image.hf.space

# =============================================================================
# CONFIGURACIÓN CORS
# =============================================================================
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://helpova.web.app
CORS_MAX_AGE=3600

# =============================================================================
# CONFIGURACIÓN JWT (CRÍTICA PARA SEGURIDAD)
# =============================================================================
# Este backend rota la clave de firma JWT cada 30 minutos de forma determinística.
# Se deriva de una clave base (JWT_SECRET_BASE) y se añade un 'kid' en el header.
#
# Para DESARROLLO puedes mantener JWT_SECRET_BASE con un valor fijo.
# En PRODUCCIÓN genera una clave segura con: openssl rand -hex 64
JWT_SECRET_BASE=dev_base_secret_change_in_production_2025
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
DB_NAME=ovaweb_dev

# ===== PRODUCTION ONLY (uncomment and set in prod) =====
# ENVIRONMENT=production
# USE_SQLITE=false
# ALLOWED_ORIGINS=https://helpova.web.app,https://api.ovaonline.tech,https://www.api.ovaonline.tech
# JWT_SECRET_BASE=replace_with_strong_random_key
# HF_API_KEY=your_production_hf_api_key
# HF_TOKEN=${HF_API_KEY}

# ===== INSTRUCTIONS =====
# 1. Copia este archivo como .env en la carpeta raíz del backend
# 2. Rellena valores reales y NO subas .env al repositorio
# 3. En producción usa variables de entorno (no archivo .env)
